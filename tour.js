
const showCase=document.querySelector('.form');
showCase.addEventListener('submit', showMessage)


function showMessage(){
         alert("Your message was sent")
      
}

/*

const arrow=document.querySelector('.arrow');

window.addEventListener('scroll',(e) => {
    const scrolled=window.scrollY;
    if(scrolled>=600){
        arrow.style.classList.add('activee')    }
    else{
        arrow.style.classList.remove('activee')

    }
});
*/
